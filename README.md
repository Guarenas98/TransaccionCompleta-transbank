# TransaccionCompleta-transbank
Muestra rapida de transaccion completa usando flask y transbank-sdk.

Requisito:
transbank-sdk (pip install transbank-sdk)
flask (pip install Flask)

Ejecutar: python3 server_trans.py
